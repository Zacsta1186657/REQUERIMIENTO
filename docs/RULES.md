### REGLAS A SEGUIR

# LOGISTICA
En el rol de `LOGISTICA` cuando llega un requerimiento. Quiero que consideres lo que es `ENVIOS PARCIALES` ahora te explico a que me refiero, lo que pasa que en esta logica, el de Area de Logistica puede ver veces que no envie todos los itemspara que se despache porque puede que no los tenga o los este comprando entonces ante ello lo que quiero hacer es poder enviar lo que se tenga y lo que alta quede en eun estado pendiente, que no se cierre si no que este en estado pendiente hasta que se termine de enviar todo y ahi si se cerraria lo que es el estado pendiente a completado. Pero para eso tambien quiero que me des una idea de como podria ser y como se podria implementar
